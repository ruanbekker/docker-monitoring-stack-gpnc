apiVersion: 1

apps:
  - type: "grafana-lokiexplore-app"
    org_id: 1
    org_name: "Grafana"
    disabled: false
    jsonData:
      apiUrl: http://localhost
      isApiKeySet: true
    secureJsonData:
      apiKey: example-key
